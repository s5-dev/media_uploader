name: s5_media_uploader
description: Processes and uploads media to the S5 Network
version: 1.0.0

environment:
  sdk: ^3.2.3

dependencies:
  lib5: ^0.1.0

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
